{
    "version": "2.0.0",
    "tasks": [
        {
            "type": "go",
            "label": "build default",
            "command": "build",
			"args": [
				"-o",
				"..\\bin\\tilinghelper-${config:from_branch}_${config:to_branch}.exe"
			],
			"options": {
				"cwd": "${workspaceFolder}\\src"
			},
            "group": "build"
        }
    ]
}
