{
    "version": "2.0.0",
    "tasks": [
        {
            "type": "go",
            "label": "build default",
            "command": "build",
			"args": [
				"-o",
				"..\\bin\\tilinghelper-${env:branch}.exe"
			],
			"options": {
				"cwd": "${workspaceFolder}\\src"
			},
            "group": "build"
        }
    ]
}
